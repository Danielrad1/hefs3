/**
 * Shared types for in-memory database repositories
 */

export interface StatsResult {
  newCount: number;
  learningCount: number;
  reviewCount: number;
  totalCards: number;
  totalNotes: number;
  totalDecks: number;
}
